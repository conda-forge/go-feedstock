Limit CGo internal-link tests to toolchains that support them.

Opt in only from the validated Windows ARM64 MSVC recipe test environment.
Pure-Go internal linking and external CGo tests remain enabled.

--- a/src/cmd/dist/test.go
+++ b/src/cmd/dist/test.go
@@ -1232,6 +1232,9 @@
 }
 
 func (t *tester) internalLink() bool {
+	if goos == "windows" && goarch == "arm64" && os.Getenv("GO_TEST_MSVC_EXTERNAL_ONLY") == "1" {
+		return false // MSVC-target C objects are not supported by the internal linker.
+	}
 	if gohostos == "dragonfly" {
 		// linkmode=internal fails on dragonfly since errno is a TLS relocation.
 		return false
--- a/src/internal/testenv/testenv.go
+++ b/src/internal/testenv/testenv.go
@@ -353,6 +353,9 @@
 // CanInternalLink reports whether the current system can link programs with
 // internal linking.
 func CanInternalLink(withCgo bool) bool {
+	if withCgo && runtime.GOOS == "windows" && runtime.GOARCH == "arm64" && os.Getenv("GO_TEST_MSVC_EXTERNAL_ONLY") == "1" {
+		return false
+	}
 	return !platform.MustLinkExternal(runtime.GOOS, runtime.GOARCH, withCgo)
 }
 
--- a/src/cmd/internal/script/scripttest/conditions.go
+++ b/src/cmd/internal/script/scripttest/conditions.go
@@ -71,6 +71,9 @@
 func cgoLinkExt(s *script.State) (bool, error) {
 	GOOS, _ := s.LookupEnv("GOOS")
 	GOARCH, _ := s.LookupEnv("GOARCH")
+	if externalOnly, _ := s.LookupEnv("GO_TEST_MSVC_EXTERNAL_ONLY"); GOOS == "windows" && GOARCH == "arm64" && externalOnly == "1" {
+		return true, nil
+	}
 	return platform.MustLinkExternal(GOOS, GOARCH, true), nil
 }
 
--- a/src/cmd/go/script_test.go
+++ b/src/cmd/go/script_test.go
@@ -291,6 +291,7 @@
 }
 
 var extraEnvKeys = []string{
+	"GO_TEST_MSVC_EXTERNAL_ONLY", // conda: preserve the selected CGo test capabilities
 	"SYSTEMROOT",         // must be preserved on Windows to find DLLs; golang.org/issue/25210
 	"WINDIR",             // must be preserved on Windows to be able to run PowerShell command; golang.org/issue/30711
 	"LD_LIBRARY_PATH",    // must be preserved on Unix systems to find shared libraries
--- a/src/cmd/go/testdata/script/cgo_suspect_flag_force_external.txt
+++ b/src/cmd/go/testdata/script/cgo_suspect_flag_force_external.txt
@@ -50,9 +50,10 @@
 env CGO_CFLAGS=
 
 # Second build uses CGO, so we expect to see the token present in the
-# -n output only when strange flags are used.
-go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+# -n output only when strange flags are used, unless the toolchain always
+# requires external CGo linking.
+go build -x -n -o dummy.exe ./usesInternalCgo
+[!cgolinkext] ! stderr preferlinkext
 env CGO_CFLAGS=-flto
 go build -x -n -o dummy.exe ./usesInternalCgo
 stderr preferlinkext
@@ -66,36 +67,36 @@
 # Trimming file information for the UndefinedBehaviorSanitizer is permitted for internal linking.
 env CGO_CFLAGS=-fsanitize-undefined-strip-path-components=-1
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 env CGO_CFLAGS=-fsanitize-undefined-strip-path-components=2
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 
 # The -fdebug-prefix-map=path is permitted for internal linking.
 env CGO_CFLAGS=-fdebug-prefix-map=/some/sandbox/execroot/workspace=/tmp/new
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 env CGO_CFLAGS=-fdebug-prefix-map=/Users/someone/.cache/bazel/_bazel_someone/3fa7e4650c43657ead684537951f49e2/sandbox/linux-sandbox/10/execroot/rules_go_static=.
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 # The -ffile-prefix-map=path is permitted for internal linking too.
 env CGO_CFLAGS=-ffile-prefix-map=/Users/someone/.cache/bazel/_bazel_someone/3fa7e4650c43657ead684537951f49e2/sandbox/linux-sandbox/10/execroot/rules_go_static/bazel-out/aarch64-fastbuild-ST-b33d65c724e6/bin/external/io_bazel_rules_go/stdlib_=.
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 # Verifying that -fdebug-prefix-map=path, -ffile-prefix-map, -no-canonical-prefixes
 # and -fno-canonical-systemd-headers are permitted for internal linking.
 env CGO_CFLAGS=-fdebug-prefix-map=old=/tmp/new
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 env CGO_CFLAGS=-ffile-prefix-map=/Users/someone/_11233/things=new
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 env CGO_CFLAGS=-no-canonical-prefixes
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 env CGO_CFLAGS=-fno-canonical-system-headers
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 env CGO_CFLAGS=
 
 # CGO_LDFLAGS with -static variants are not compatible with internal linking.
@@ -119,7 +120,7 @@
 # CGO_LDFLAGS=-static-libgcc does NOT trigger external linking.
 env CGO_LDFLAGS=-static-libgcc
 go build -x -n -o dummy.exe ./usesInternalCgo
-! stderr preferlinkext
+[!cgolinkext] ! stderr preferlinkext
 env CGO_LDFLAGS=
 
 # CGO_CFLAGS=--static triggers external linking (some toolchains accept it).
